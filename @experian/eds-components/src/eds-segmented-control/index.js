import { addStyle } from '../eds-core/styles';
const css = require('./eds-segmented-control.scss');

// This is a CSS Only component so just expose the CSS styles.
addStyle('eds-segmented-control', css);
